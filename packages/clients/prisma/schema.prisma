generator client {
  provider = "prisma-client-js"
  // previewFeatures = ["dataProxy"]
}

datasource db {
  provider = "postgresql"
  url      = "REDACTED"
}

model user {
  id String @id
  firstName String
}
